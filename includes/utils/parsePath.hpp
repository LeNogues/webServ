#ifndef PARSEPATH_HPP
# define PARSEPATH_HPP

# include <string>
# include <map>
# include <algorithm>

bool backPath(std::string& path);
std::string	guessContentType(const std::string& path);

#endif
